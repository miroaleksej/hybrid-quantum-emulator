# requirements.txt

## Core dependencies
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.4.0
networkx>=2.6.0
gudhi>=3.5.0
ripser>=0.6.0
scikit-learn>=1.0.0
numba>=0.53.0
tqdm>=4.62.0
pandas>=1.3.0
seaborn>=0.11.0

## Topology analysis
persim>=0.2.0
kmapper>=1.4.1
tda-tools>=0.1.0

## GPU acceleration
cupy-cuda11x>=10.0.0 ; sys_platform == 'linux'
pycuda>=2021.1
torch>=1.10.0
torchvision>=0.11.0
torchaudio>=0.10.0

## Quantum framework integration
qiskit>=0.32.0
qiskit-aer>=0.9.0
qiskit-ibmq-provider>=0.18.0
cirq>=0.11.0
pennylane>=0.19.0
quantum-gates>=0.1.0

## Testing
pytest>=6.2.0
pytest-cov>=3.0.0
pytest-asyncio>=0.16.0
hypothesis>=6.27.0
mock>=4.0.0
coverage>=6.0.0

## Documentation
sphinx>=4.2.0
sphinx-rtd-theme>=1.0.0
nbsphinx>=0.8.0
m2r2>=0.3.0
recommonmark>=0.7.0
sphinxcontrib-napoleon>=0.7.0
sphinx-autobuild>=2021.3.14

## Build and deployment
setuptools>=58.0.0
wheel>=0.37.0
twine>=3.4.0
Cython>=0.29.0
docker>=5.0.0
pyyaml>=5.4.0

## Optional dependencies (for specific platforms)
# Windows dependencies
pywin32>=301 ; sys_platform == 'win32'
# macOS dependencies
pyobjc-core>=8.0 ; sys_platform == 'darwin'
pyobjc-framework-Cocoa>=8.0 ; sys_platform == 'darwin'

## Development tools
black>=21.9b0
flake8>=3.9.0
isort>=5.9.0
pre-commit>=2.15.0
jupyterlab>=3.2.0
ipykernel>=6.4.0

# Notes:
# 1. For GPU acceleration on Linux, cupy-cuda11x is specified. Adjust CUDA version as needed.
# 2. For Windows/MacOS without GPU support, install cupy-cuda11x alternatives or use cupy (CPU-only).
# 3. To install with GPU support: pip install -r requirements.txt
# 4. For minimal installation (core only): pip install -r requirements.txt --no-deps
# 5. For development environment: pip install -r requirements.txt[dev]
# 6. For documentation building: pip install -r requirements.txt[docs]
